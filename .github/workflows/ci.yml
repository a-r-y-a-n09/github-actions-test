name: CI with Cache Demo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: prime-cache-729a12b
      uses: actions/cache@v4
      with:
        key: cache-729a12b
        path: node_modules
        restore-keys: |
          ${{ runner.os }}-npm-
